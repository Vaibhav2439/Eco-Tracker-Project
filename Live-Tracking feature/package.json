{
  "name": "carbon-eco",
  "version": "1.0.0",
  "description": "Carbon Eco — Activity tracking demo (root orchestrator)",
  "scripts": {
    "install-all": "cd backend && npm install",
    "start": "cd backend && npm run dev",
    "default": "npm  start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
